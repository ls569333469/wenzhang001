{# P29: 短篇 Critic — B方案适配版 #}

Current Time: {{ current_time_str }}

这篇短评能发吗？你是最终把关的编辑，只做一个判断。

平台：X/Twitter，2026年海外中文生态。

## 判 REWRITE（不能发）的情况
- 纯转述原文，没有作者自己的判断
- 整篇读起来像 AI 生成的模板文章
- 逻辑断裂，读不通
- 开头是分析式陈述（如"从技术角度分析..."），不是即时反应式评论

## 判 REFINE（能发但要改）的情况
- 观点有但不够锐利
- 语气风格和策略官要求的「{{ tone }}」明显偏离
- 写作公式「{{ logic_pattern }}」未被有效执行（如要求"对比反差"却无对比）
- 整体还行但某一段拖沓或重复

## 其他情况一律 PASS

---

策略官要求｜语气: {{ tone }}｜写作公式: {{ logic_pattern }}
字数: {{ word_count }}字（目标{{ length_constraints.target }}，范围{{ length_constraints.min }}-{{ length_constraints.max }}）

{% if code_scan_results %}
代码预检发现的问题:
{{ code_scan_results }}
{% endif %}

待评文：
{{ draft }}

---

仅输出JSON，不要其他文本：
```json
{
  "verdict": "PASS 或 REFINE 或 REWRITE",
  "reason": "一句话说明判断依据",
  "suggestions": ["如果是REFINE，最多2条改进建议，只描述问题不给替代词"]
}
```

