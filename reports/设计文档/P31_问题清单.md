# P31 投研模块 — 问题清单

> 更新: 2026-02-28 00:14 | 首次日报全流程跑通后整理

## 核心问题总结

当前状态：**后端管线跑通（日报文件已生成），但前端体验完全断开**。
用户点击「生成日报」后，屏幕上看不到任何变化。

---

## 🔴 P0 — 必须解决

| # | 问题 | 说明 | 影响范围 |
|---|------|------|---------|
| 1 | **主区域画布无任何变化** | 点「生成日报」后，中间画布（输入框+输出区）完全没有更新。日报结果只打了 `console.log`，用户看不到任何输出。 | `ResearchPanel.tsx` + 主区域组件 |
| 2 | **右栏思考过程无更新** | 日报走的是独立 REST API，没有对接右栏 ThinkingSteps。用户完全不知道当前进度。 | API 需改 SSE 或 WebSocket |
| 3 | **日报结果无法在前端查看** | 日报保存在 `reports/research/daily_research_20260227.md`，但前端没有渲染入口。 | 需要主区域展示 Markdown |
| 4 | **无配图生成** | 旧版 P31 有 `card_*.html` 配图，新版 `daily_report_service.py` 完全没有配图逻辑。 | 需要加入配图生成步骤 |

## 🟡 P1 — 需要改进

| # | 问题 | 说明 |
|---|------|------|
| 5 | KOL 关注数全部为 0 | `_parse_projects_from_text` 正则未匹配到 Surf 输出格式 |
| 6 | 侦察官搜索耗时 ~2 分钟无进度 | 按钮有 loading 但无倒计时/进度条 |
| 7 | 生成日报耗时 ~14 分钟无进度 | 用户不知道进行到哪一步（侦察/策略/审核/写手/润色） |
| 8 | 右栏 ThinkingSteps UI 和 HTML 原型不一致 | 现有右栏是通用的 LangGraph 步骤展示，需要投研专用的进度 UI |

## 🟢 P2 — 优化项

| # | 问题 | 说明 |
|---|------|------|
| 9 | 项目类别粗略（只有 AI/Web3） | scout prompt 需要强制返回精确赛道 |
| 10 | 素材中心投研报告标签页未实施 | 方案第 4 项，需要新增 `ResearchReports.tsx` |
| 11 | 定时任务未接入 | 每日自动触发 daily-report |

---

## ✅ 已修复

| # | 问题 | 修复 |
|---|------|------|
| A | `REPORTS_DIR` 路径错误 | 加一层 `.parent` |
| B | `GET /scout` 阻塞 event loop | 改为 `asyncio.to_thread` |

## ✅ 正常工作

- 后端全流程：侦察官 → 策略官×6（并发3）→ 审核官 → 写手 → 润色官 ✅
- 文件保存：日报 + 侦察结果 + 6 个项目报告 ✅
- API 端点：`GET /scout`、`POST /daily-report`、`GET /reports` ✅
- 前端侦察官搜索按钮 + 项目卡片勾选 ✅
