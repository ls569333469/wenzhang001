# P27 模式重构规划

> 创建时间: 2026-02-19 | 更新: 2026-02-20
> 状态: 规划完成 | 2/20 前端全面审计 + 架构确认方案B (DataPanel 左右分栏)

## 总览

删除无效控件和废弃模式，优化现有模式，新增 3 个业务模式，**并全面清理前端中不生效的功能**。

### 模式变更表

| # | 模式 | ID | 变更 | 状态 |
|---|------|-----|------|------|
| 1 | 🔥 锐评 | `hot_take` | 改走完整管线 + 优化提示词 | 待测试确定方向 |
| 2 | 🌸 吹捧 | `bullish_take` | **新增** — 币安/CZ/何一正面解读 | 已确认方案 |
| 3 | 🎯 Kaito | `kaito_yap` | **新增** — 项目嘴撸任务 | 已确认方案 |
| 4 | 💎 短篇 | `short_article` | 不变 | ✅ |
| 5 | ⬛ 中篇 | `mid_article` | 不变 | ✅ |
| 6 | 📖 长篇 | `long_article` | 不变 | ✅ |
| 7 | 📚 教程 | `tutorial` | 深度完善 | ⏸️ 延后 |
| 8 | 🔬 项目投研 | `project_research` | **新增** | 已确认方案 |
| ~~✏️~~ | ~~改写润色~~ | ~~`rewrite`~~ | **删除**（占位符，从未使用） | 待执行 |

### UI 变更

| 控件 | 变更 |
|------|------|
| 开头吸引力（4档选择） | **删除** — 底层是 `retention_level`，与 UI 语义不匹配，实际无效果 |

### 架构决策

- 锐评 / 吹捧 / Kaito **保持 3 个独立模式**，各自有独立的 Writer、Prompt 模板和配置
- 底层可共用工具函数（Google Sheet 拉取、数据格式化等），但模式入口分开
- 理由：方便日后独立更新维护，避免耦合

### 前端架构：方案B（DataPanel + WritingCanvas 左右分栏）⭐

> 详细方案见: [P27_前端优化设计方案.md](file:///d:/AI_Projects/2026001/reports/设计文档/P27_前端优化设计方案.md) § 2.1-2.4

**核心变更**: ConfigPanel **只放配置项**（模式/风格/篇幅），模式相关的数据展示放入新建的 **DataPanel** 组件，与 WritingCanvas 左右并排。

**布局结构**:
```
┌ ConfigPanel ┐ ┌ DataPanel (新)  ┐ ┌ WritingCanvas ┐ ┌ RightSidebar ┐
│ (180px)      │ │ (~40% 可拖拽)  │ │ (flex-1)      │ │ (288px)      │
│ 模式选择     │ │ 吹捧→ Feed     │ │ RichEditor   │ │ 进度/版本    │
│ 写作风格     │ │ Kaito→ 看板   │ │ SSE 流式     │ │ 日志/导出    │
│ 篇幅长度     │ │ 投研→ 搜索    │ │ 3版本        │ │              │
│ ✅ 只放配置   │ │ 普通→ 不渲染  │ │ ✅ 零修改    │ │ ✅ 保留      │
└─────────────┘ └───────────────┘ └──────────────┘ └─────────────┘
```

**模式 → DataPanel 映射**:

| 模式 | DataPanel 内容 | ConfigPanel 内容 |
|------|---------------|---------------|
| 锐评 / 短篇 / 中篇 / 长篇 | **不渲染** (WritingCanvas 独占全宽) | 风格 + 字数 |
| 吹捧 | `BullishFeed.tsx` — CZ/何一/Binance 实时动态 | 吹捧体 + 篇幅 |
| 嘴撸 (Kaito) | `KaitoBoard.tsx` — 项目看板 + 角度推荐 + 重复提醒 | 嘴撸体 + 篇幅 |
| 投研 | `ResearchPanel.tsx` — 搜索 + 数据源 + 市场概览 | 输出格式 |

**WritingCanvas 零影响**: RichEditor 内部代码不改，仅 `StudioLayout.tsx` 加 flex 分栏 + `max-w-[960px]` → `w-full` 一行。

**涉及重构**:
- `ConfigPanel.tsx` (580行) 拆分为: 主框架 + `ModeStandard` / `ModeBullish` / `ModeKaito` / `ModeResearch`
- 新建 `DataPanel.tsx` + `BullishFeed.tsx` / `KaitoBoard.tsx` / `ResearchPanel.tsx`
- `StudioLayout.tsx` 加 flex 分栏容器
- `constants.ts` 删除 `WEB3_KNOWLEDGE_BASES`，新增 3 模式定义

**设计稿**: [P27_完整页面预览.html](file:///d:/AI_Projects/2026001/reports/设计文档/P27_完整页面预览.html)(全交互) | [P27_configpanel_mockup.html](file:///d:/AI_Projects/2026001/reports/设计文档/P27_configpanel_mockup.html)

---

## 前端全面审计 🔍（2/20 新增）

### 审计结论：6 个不生效的 ConfigPanel 控件

对后端做了全面代码搜索，确认以下控件前端有 UI 但后端**完全不读取**：

| 控件 | 前端位置 | 后端读取 | 结论 |
|------|---------|---------|------|
| 🌡️ Temperature 滑块 | `ConfigPanel.tsx:279-295` | ❌ 每个 Agent 硬编码自己的 temp（策略师 0.85, 短篇 0.88, 审核 0.1 等） | **摆设** |
| 📊 Top P 滑块 | `ConfigPanel.tsx:297-311` | ❌ 后端 `llm.py` 底层无 top_p 参数 | **摆设** |
| 📏 Max Tokens 输入 | `ConfigPanel.tsx:313-324` | ❌ 各 Writer 根据字数自动计算 max_tokens | **摆设** |
| 🪝 开头吸引力 (Hook) | `ConfigPanel.tsx:182-206` | ❌ 底层 `retention_level` 语义不匹配 | **摆设 → 已确认删除** |
| 📐 内容保留度 (Retention) | `ConfigPanel.tsx:225-255` | ❌ 后端无对应逻辑 | **摆设** |
| 🧠 知识库选择 | `ConfigPanel.tsx:259-270` | ❌ 后端不读 `knowledgeSources` 字段 | **摆设** |

**结论：整个「高级设置」折叠区可以直接删除。**

---

### 审计结论：各页面功能状态

| 页面 | 路由 | 功能状态 | 问题 | P27 决定 |
|------|------|---------|------|----------|
| 🟢 **创作中心** | `/studio` | **核心功能，正常工作** | ConfigPanel 需重构 | 拆分 ConfigPanel + 新增 DataPanel |
| 🟢 **系统设置** | `/settings` | **功能正常** | 含 `rewrite` 模式需清理 | P0 删除 rewrite，新增 3 模式 |
| 🟡 **仪表盘** | `/dashboard` | 仅 2 张卡片 + 3 快捷入口 | 功能单薄 | **保留并增强** |
| 🔴 **知识库** | `/knowledge` | Lark 数据清洗 | 与创作脱节 | 合并为 `/data` |
| 🔴 **数据清洗** | `/cleaner` | 与 Knowledge 重叠 | 无侧边栏入口 | 合并为 `/data` |
| 🔴 **智能体团队** | `/agents` | 纯只读摆设 | 所有操作跳 settings | **删除** |

**侧边栏其他问题**：

| 问题 | 位置 | 说明 |
|------|------|------|
| 「最近项目」硬编码 | `AppSidebar.tsx:123-132` | "DeFi 周报 (草稿)" 和 "Layer 2 深度分析" 是写死的假数据 |
| 用户名硬编码 | `AppSidebar.tsx:140` | "Web3 Master" 写死 |
| Cleaner 无入口 | 侧边栏 | `/cleaner` 页面存在但侧边栏没有导航链接 |
| StudioNavbar 过时 | `StudioNavbar.tsx:37-40` | 还含 agents/knowledge pill，需同步更新 |

---

### 前端优化方案：P0 / P1 / P2 分级

#### 🥇 P0 — 清理摆设（无依赖，可立即执行）

| # | 任务 | 涉及文件 |
|---|------|---------|
| 1 | ~~删除改写润色模式~~ | `constants.ts` (CREATION_MODES), `schema.ts` (CreationMode), `AgentModelConfig.tsx` (MODE_DEFINITIONS), `backend/app/agents/writer/rewrite.py`, `backend/data/prompts/writer/rewrite.jinja2`, `backend/app/api/rewrite.py` |
| 2 | ~~删除整个「高级设置」折叠区~~ | `ConfigPanel.tsx` 删除 Temperature / Top P / Max Tokens 的 UI + URL state |
| 3 | ~~删除「开头吸引力」控件~~ | `ConfigPanel.tsx` 删除 Hook Intensity UI, `constants.ts` 删除 `HOOK_INTENSITIES` |
| 4 | ~~删除「内容保留度」控件~~ | `ConfigPanel.tsx` 删除 Retention Level UI + URL state |
| 5 | ~~删除「知识库选择」假数据~~ | `ConfigPanel.tsx` 删除 KnowledgeSelector, `constants.ts` 删除 `WEB3_KNOWLEDGE_BASES` |
| 6 | ~~删除侧边栏硬编码假数据~~ | `AppSidebar.tsx` 删除 "DeFi 周报 / Layer 2 深度分析" 假项目 |
| 7 | ~~删除智能体团队页面~~ | `/agents/page.tsx` 删除（或改为 redirect 到 `/settings`），侧边栏移除入口 |

> **预估工作量：1-2 小时**，全部是删除操作，无新功能开发。

#### 🥈 P1 — ConfigPanel + DataPanel 重构

| # | 任务 | 说明 |
|---|------|------|
| 1 | ConfigPanel 拆分 | 580 行单文件 → 主框架 + `ModeStandard` / `ModeBullish` / `ModeKaito` / `ModeResearch` 子组件 |
| 2 | 新建 DataPanel | `DataPanel.tsx` + `BullishFeed.tsx` / `KaitoBoard.tsx` / `ResearchPanel.tsx` |
| 3 | StudioLayout 改造 | `<main>` 加 flex 分栏 + WritingCanvas `max-w-[960px]` → `w-full` |
| 4 | 新增 3 个模式到 schema | `bullish_take` / `kaito_yap` / `project_research` 加入 `CreationMode` 类型 |
| 5 | constants.ts 更新 | `CREATION_MODES` 新增 3 个模式定义 |
| 6 | 锐评迁移 LangGraph | 从独立 `/hot-take` API 合并到标准管线 |
| 7 | Settings 页模型配置同步 | `AgentModelConfig.tsx` 的 `MODE_DEFINITIONS` 适配新模式 |
| **8** | **⭐ 状态联动** | `useAgentStore` 新增 `selectedMaterial` + `isDirty`，三面板共享 Store（终审反馈 #2） |
| **9** | **⭐ 空状态设计** | DataPanel 加 Skeleton 骨架屏 + Empty State（终审反馈 #3） |
| **10** | **⭐ 模式切换守卫** | `setMode()` 判断 isDirty，自动保存草稿 + Toast 提示（终审反馈 #4） |

#### 🥉 P2 — 页面整合优化

| # | 任务 | 说明 |
|---|------|------|
| 1 | Knowledge + Cleaner 页面合并 | 两个页面功能重叠，合并为 `/data` 数据管理 |
| 2 | 仪表盘增强 | 丰富为完整仪表盘（API状态、创作统计、快速入口） |
| 3 | StudioNavbar 同步更新 | 删除 agents pill、knowledge → data、更新 UI_TEXT 常量 |
| 4 | 侧边栏「最近项目」接真数据 | 接 `creation_store` API，显示用户真实保存的历史创作 |
| 5 | 用户名配置化 | 从 `.env` 或 Settings 读取，替代 "Web3 Master" 硬编码 |

---

## 1. 锐评优化

**现状**: 基本可用，目前走独立路径（不经过 LangGraph）。

**关键变更**: **改走完整 LangGraph 管线**（策略 → 写作 → 审核 → 润色），与短篇/中篇对齐。

**优化方向**:
- 迁移短篇验证过的人味化/反AI痕迹技巧
- 禁用词库 (`forbidden_patterns.yaml`) 确保生效
- 3 条候选的角度差异化
- 锐评"公式"模板（反问型、数据打脸型、类比型等）

**下一步**: 跑测试 → 确定具体问题 → 针对性优化提示词

---

## 2. 吹捧模式

**定位**: 针对**币安 / CZ / 何一**的正面解读。吹到心坎里，不是水文。

**字数**: 50-500 字，由目标内容决定

**核心原则**: 基于真实事件吹捧，不能瞎吹 — 需要实时素材支撑。

**数据源**:
- **推特**: 通过 Grok API（✅ 已有），拉取 @cz_binance / @heyibinance 等账号动态
- **媒体**: 复用现有爬虫，增加币安/CZ/何一关键词过滤
- 抓取结果存入 Google Sheet `binance_daily` Tab（上限 200 Tab，当前仅 3-4 个）
- 人工定期审核入库质量

**生成流程**: 用户输入话题 → 拉取 `binance_daily` 最新素材 → LLM 正面解读 → 输出 3 条候选

**Web2 吹捧风格素材库**: 暂不需要，后续按需补充

**代码架构**: 独立 Writer + Prompt 模板，走完整 LangGraph 管线

---

## 3. Kaito 嘴撸任务模式

**定位**: 针对项目方长期内容任务，每天输出不同角度的正面内容。本质是商业广告/水文。

**与吹捧区别**: 吹捧 = 走心、精品；Kaito = 水文、量产

**数据源**:
- Google Sheet 按项目分 Tab（如 `kaito_berachain`、`kaito_monad`）
- 每个 Tab 维护该项目的日常动态
- 数据来源同吹捧模式（Grok API + 爬虫）

**生成流程**:
1. 用户选择项目 → 拉取该项目近 N 天的动态数据
2. LLM 分析近期内容，找出未覆盖的角度
3. 生成 3 条不同角度的候选内容
4. 避免与历史已发内容撞角度

**前端**: 比吹捧模式多一个"选择/输入项目"的入口

---

## 4. 教程深度完善 ⏸️ 延后

依赖完整爬虫 + 图片处理能力，复杂度高，P27 不做。

---

## 5. 项目投研

**定位**: 新项目速览 + 投研分析报告。

**数据源**:
- **项目发现**: [leak.me](https://leak.me/)
- **投研数据**: 先用 Grok + Gemini 替代（Surf API 后续补接）
- **数据验证**: Grok API

**两层输出**:
1. **一句话 Alpha**: 快速判断（格式待调研 X 上常见格式后优化）
2. **项目分析报告**: 团队、融资、代币经济学、链上数据等

**生成流程**:
1. 用户输入项目名/合约地址
2. Grok/Gemini 获取项目信息
3. LLM 生成一句话 Alpha + 结构化分析报告

---

## 6. 删除改写润色

占位符模式，从未使用，直接删除。

**涉及清理**:
- `backend/app/agents/writer/rewrite.py`
- `backend/data/prompts/writer/rewrite.jinja2`
- `backend/app/api/rewrite.py`
- 前端模式选项 & schema
- `AgentModelConfig.tsx` 中 `MODE_DEFINITIONS` 的 rewrite 条目
- `main.py` 中相关路由（如有）

---

## 7. 删除「开头吸引力」控件

**涉及清理**:
- `ConfigPanel` 中的 hook intensity UI
- `constants.ts` 中的 `HOOK_INTENSITIES`
- `retention_level` 硬编码为默认值 3

---

## 依赖与准备清单

### ✅ 已有

| 资源 | 状态 |
|------|------|
| Grok API | ✅ 用户已有 |
| Gemini API | ✅ 系统已接入 |
| Google Sheet 基础设施 | ✅ 当前 3-4 Tab / 上限 200 Tab |
| 媒体爬虫（1 个源） | ✅ 运行中 |

### ⚠️ 需要准备

| 资源 | 用途 | 优先级 |
|------|------|--------|
| Grok API 接入代码 | 吹捧/Kaito 数据拉取 | P1 - 吹捧和 Kaito 的前置依赖 |
| Google Sheet 新 Tab 结构 | `binance_daily` + `kaito_{项目}` | P1 |
| 定时任务基础设施 | 每日自动拉取数据 | P1 - 目前无，全靠手动 |
| X API（备选） | 推特数据拉取 | P2 - Grok 可替代 |
| Surf API | 项目投研数据 | P2 - 等用户拿到 |

### 🔨 需要开发

| 模块 | 说明 |
|------|------|
| Grok API 适配器 | 对接 Grok API，支持推特搜索/账号动态 |
| 定时任务调度器 | 每日自动执行数据抓取脚本 |
| 锐评 → LangGraph 迁移 | 从独立路径改为走完整管线 |
| 3 个新 Writer + Prompt | bullish_take / kaito_yap / project_research |

---

## 优先级排序（更新版 2/20）

| 优先级 | 任务 | 依赖 | 预估工时 |
|--------|------|------|---------|
| 🥇 **P0** | ~~前端清理（删改写润色 + 删高级设置 + 删Hook + 删Retention + 删知识库假数据 + 删侧边栏假数据 + 删智能体页面）~~ | ✅ 完成 | **1-2h** |
| 🥈 P1 | ConfigPanel 拆分 + **DataPanel 新建** + StudioLayout 改造 + 3 模式 schema | P0 先清理干净 | **6-8h** |
| 🥈 P1 | Grok API 适配器 + 定时任务基础设施 | Grok API Key | 4-6h |
| 🥈 P1 | 锐评改走完整管线 + 提示词优化 | 跑测试确定方向 | 3-4h |
| 🥈 P1 | 吹捧模式 | Grok 适配器 + binance_daily Tab | 4-6h |
| 🥈 P1 | Kaito 模式 | Grok 适配器 + 项目 Tab | 4-6h |
| 🥉 P2 | Knowledge + Cleaner 合并为 `/data` | 确定数据管理定位 | 2-3h |
| 🥉 P2 | 仪表盘增强 + StudioNavbar 更新 | P2 页面整合 | 2-3h |
| 🥉 P2 | 侧边栏接真数据 + 用户名配置化 | creation_store API | 1-2h |
| 🥉 P2 | 项目投研 | Grok + Gemini（先行）/ Surf API（后续） | 6-8h |
| ⏸️ | 教程深度完善 | 爬虫太复杂，延后 | - |
