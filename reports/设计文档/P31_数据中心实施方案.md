# P31+ 数据中心 `/data` 实施方案 (v3 — 完整影响分析)

## 一、路由引用排查结果

### `/research` 路由引用（共 2 处导航）

| 文件 | 行号 | 当前内容 | 改动 |
|------|------|---------|------|
| `AppSidebar.tsx` | L33 | `{ href: "/research", label: "投研报告" }` | **删除该项** |
| `research/page.tsx` | L14 | `router.replace('/studio?mode=project_research')` | **保留**（兜底重定向） |

### `/knowledge` 路由引用（共 3 处导航）

| 文件 | 行号 | 当前内容 | 改动 |
|------|------|---------|------|
| `Navbar.tsx` | L23 | `{ label: '知识库', href: '/knowledge' }` | → `{ label: '数据中心', href: '/data' }` |
| `Dashboard/page.tsx` | L98-100 | `title="知识库管理" href="/knowledge"` | → `title="数据中心" href="/data"` |
| `data/page.tsx` | L23 | `href="/knowledge"` 链接 | **整页重写**（Tab 容器） |

### `/data` 路由引用（共 2 处 — 已正确）

| 文件 | 行号 | 当前内容 | 改动 |
|------|------|---------|------|
| `AppSidebar.tsx` | L34 | `{ href: "/data", label: "数据管理" }` | label → "数据中心" |
| `StudioNavbar.tsx` | L38 | `label="数据管理" href="/data"` | label → "数据中心" |

### `/cleaner` 路由引用（0 处导航）

> `/cleaner` 没有任何导航入口指向它，它的引用全在自身页面内（API 调用 `/cleaner/*`）。
> 不影响 Phase 1，Phase 2 迁移时处理。

### `MaterialCenter` 引用（1 处）

| 文件 | 行号 | 当前内容 | 改动 |
|------|------|---------|------|
| `studio/page.tsx` | L9,30 | `import { MaterialCenter }` + 渲染 | Phase 1 **不动**，Phase 2 迁移 |

---

## 二、Phase 1 完整改动清单

| # | 文件 | 操作 | 具体改动 |
|---|------|------|---------|
| 1 | `AppSidebar.tsx` | MODIFY | 删除 `/research` 项，`/data` label → "数据中心" |
| 2 | `Navbar.tsx` | MODIFY | `/knowledge` → `/data`，label → "数据中心" |
| 3 | `StudioNavbar.tsx` | MODIFY | label "数据管理" → "数据中心" |
| 4 | `Dashboard/page.tsx` | MODIFY | "知识库管理" → "数据中心"，href `/knowledge` → `/data` |
| 5 | `data/page.tsx` | **REWRITE** | 占位页 → 4 Tab 容器 |
| 6 | `features/data/ResearchArchive.tsx` | **NEW** | 投研存档组件（日报+项目报告列表） |

> [!IMPORTANT]
> **不删除** `/knowledge`、`/cleaner`、`/research` 页面文件。
> 它们暂时保留，用户直接访问 URL 仍然可用。Phase 2 再迁移为组件 + 加重定向。

## 三、风险清单

| 风险 | 详情 | 对策 |
|------|------|------|
| Studio 素材 Tab 失效 | `StudioNavbar` 的 "素材" 链接 → `/studio?view=materials`，引用 `MaterialCenter` | **不动**，Phase 1 不迁移 MaterialCenter |
| `/knowledge` 直接访问 | 有人手动输入或收藏了 `/knowledge` URL | **保留页面**，Phase 2 才重定向 |
| `/cleaner` 直接访问 | 同上 | **保留页面** |
| `/research` 直接访问 | 已有重定向到 Studio | **保留**，不受影响 |
| API 路径 | `/api/research/*`、`/ingest/*`、`/cleaner/*` | **全部不变**，只改前端导航 |

## 四、验证

1. 侧边栏：4项（仪表盘、创作中心、数据中心、系统设置），无"投研报告"
2. `/data` → 4 Tab 正常切换
3. 投研存档 Tab → 日报列表 + 项目报告
4. Dashboard 快速操作 → "数据中心" 跳转正确
5. `/knowledge`、`/cleaner`、`/research` 直接访问仍正常
