# P33: 综合修复方案（审查报告 + 补充审查）

> **日期**: 2026-03-04  
> **原始审查**: 2026-03-03 22 项  
> **补充审查**: 2026-03-04 +7 项遗漏 + 5 项新增

---

## 一、审查计划 vs 审查报告 Gap 分析

| 计划编号 | 计划内容 | 报告中实际做了什么 | 状态 |
|----------|---------|-------------------|------|
| D1 | 未使用的 import | ❌ **误映射为 print()** | 🔴 遗漏 |
| D2 | 废弃的函数/组件 | 仅找到 1 个 deprecated | 🟡 不完整 |
| P2 | 日志级别 | ❌ 未检查 | ✅ 补查: `setLevel(INFO)` 已设 |
| R2 | 并发安全 | ❌ **误映射为磁盘增长** | 🟡 遗漏 |
| R4 | API 响应大小 | ❌ 未检查 | 🟡 遗漏 |

## 二、补充审查发现的新问题

| # | 问题 | 严重性 | 详情 |
|---|------|--------|------|
| **N4** | `datetime.now()` 无时区 | 🔴 严重 | **40+ 处** 使用 `datetime.now()` 无 tz 参数。VPS 在美国(UTC-5)，用户在中国(UTC+8)，日报日期、文件名、催化剂时间窗全部错 13 小时 |
| **N1** | npm 依赖高危漏洞 | 🟡 中等 | `npm audit`: **3 个 high** + 2 个 moderate |
| **N2** | TypeScript 编译 | ✅ 通过 | `tsc --noEmit` 无错误 |
| **N3** | 中文编码 | ✅ 通过 | Python3 默认 UTF-8，Linux 兼容 |
| **N5** | 输入验证 | ✅ 通过 | API 请求用 Pydantic `BaseModel` 校验 |

---

## 三、完整修复方案（按优先级排序）

### 🔴 P0 — 上线阻塞项（必须修复，不修不上线）

#### Fix-1: 创建 .gitignore（5min）
```
# 原审查 S2
# 创建项目根目录 .gitignore
backend/config/user_config.json
backend/config/google_service_account.json
backend/.env
backend/venv/
backend/__pycache__/
frontend/node_modules/
frontend/.next/
*.pyc
reports/research/projects/
```

#### Fix-2: 生成 requirements.txt（2min）
```bash
# 原审查 P5
cd backend && pip freeze > requirements.txt
```

#### Fix-3: 时区修复（15min）
```python
# 补充审查 N4
# 方案: 在 config.py 中定义统一时区常量
from datetime import timezone, timedelta
CN_TZ = timezone(timedelta(hours=8))

# 所有 datetime.now() 改为 datetime.now(CN_TZ)
# 重灾区（必改）:
#   daily_report_service.py L568, L788  — 影响日报日期、催化剂窗口
#   research_sheet.py L90, L170         — 影响 Sheets 记录时间
#   card_generator.py L53, L142, L273   — 影响配图日期
# 其他文件（可后续改）:
#   main.py (4处), graph.py (7处), agents/writer/* (8处)
```

---

### 🟡 P1 — 强烈建议修复（上线前完成）

#### Fix-4: CORS 加 VPS 地址（2min）
```python
# 原审查 S3
# main.py L47: 添加生产 URL
allow_origins=[
    "http://localhost:3000",
    "http://107.172.78.150",   # VPS IP
    # "https://你的域名",      # 配域名后加
]
```

#### Fix-5: print() → logger 批量替换（20min）
```
# 原审查 D1 (实际内容)
# 57+ 处 print() 需要替换
# 按文件分批:
#   chaincatcher.py    — 18处 → logger.info/debug
#   google_sheets_source.py — 16处 → logger.info/debug  
#   material_sheet.py  — 11处 → logger.info/debug
#   llm.py             — 4处 [LLM DEBUG] → logger.debug
#   main.py            — 多处 → logger.info
#   material_analyzer.py — 5处 → logger.info
```

#### Fix-6: Nginx 鉴权或简单 Token（10min）
```
# 原审查 S4
# 方案 A: Nginx Basic Auth（推荐，最简单）
#   htpasswd -c /etc/nginx/.htpasswd admin
#   在 nginx 配置 location / { auth_basic "Login"; ... }
#
# 方案 B: FastAPI Header Token
#   所有端点加 X-API-Key header 验证
```

#### Fix-7: SSL 验证开启（2min）
```python
# 原审查 E1-补充
# surf_service.py L75: verify=False → verify=True
```

#### Fix-8: npm 依赖更新（5min）
```bash
# 补充审查 N1
cd frontend && npm audit fix
# 如果 fix 不够: npm audit fix --force（谨慎）
```

---

### 🟢 P2 — 可上线后处理

#### Fix-9: 脚本目录清理
```
# 原审查 D3
# backend/scripts/ 41个文件 → 保留实用脚本，归档测试脚本
# 保留: setup_*.py, test_full_pipeline.py
# 归档: test_card*.py, test_surf*.py, test_tweet*.py 等
```

#### Fix-10: 前端 console.log 清理
```
# 原审查 D2
# 9 处 console.log 在 5 个文件中
```

#### Fix-11: main.py 拆分（长期）
```
# 原审查 P3-补充
# 1030行 → 拆分为 api/generation.py, api/config.py, api/ingest.py
# 删除 @deprecated generate_hot_take()
```

#### Fix-12: 报告自动清理
```bash
# 原审查 R2(实际)
# crontab 加: 每周清理 30 天前的报告
# find /reports/research -mtime +30 -name "*.md" -delete
```

---

## 四、执行计划

```
Phase 1 — P0 阻塞项（25min）
├── Fix-1: .gitignore
├── Fix-2: requirements.txt  
└── Fix-3: datetime 时区（重灾区 5 个文件）

Phase 2 — P1 强烈建议（40min）
├── Fix-4: CORS
├── Fix-5: print() → logger（57+ 处）
├── Fix-6: Nginx 鉴权方案（写配置文件，部署时用）
├── Fix-7: SSL verify
└── Fix-8: npm audit fix

Phase 3 — P2 后续（上线后）
├── Fix-9: 脚本清理
├── Fix-10: console.log 清理
├── Fix-11: main.py 拆分
└── Fix-12: 报告自动清理
```

---

## 五、风险评估

| 风险 | 概率 | 影响 | 控制措施 |
|------|------|------|---------|
| 日期错 13 小时 | 🔴 必然 | 日报日期错、催化剂窗口错 | Fix-3 时区修复 |
| API Key 泄露 | 🟡 中 | 账号被盗用 | Fix-1 .gitignore |
| 部署失败 | 🔴 必然 | 依赖装不上 | Fix-2 requirements.txt |
| API 被滥用 | 🟡 中 | 额度消耗、数据篡改 | Fix-6 鉴权 |
| npm 漏洞利用 | 🟢 低 | XSS/SSRF | Fix-8 npm audit fix |

确认后我开始执行 Phase 1。
