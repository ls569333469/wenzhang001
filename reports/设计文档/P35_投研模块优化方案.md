# P35 投研模块优化方案

> 编写时间: 2026-03-09 | 基于 P34 排查报告未完成项 + 新需求
> 最后更新: 2026-03-09 14:18

---

## 一、背景

P34 完成了投研流水线的 LLM-first 重构（删写手/润色官、加质检官、新 prompt 体系）。
P35 解决 P34 遗留的 P2 问题，并增加两个新功能。

### 当前流水线

```
侦察官(Surf) → 策略官(Surf)×N → enrichment → 质检官 → 总结官 → 推文写手 → 配图 → 日报渲染
```

---

## 二、新功能

### F1. 重新组装日报（跳过侦察+策略）

**痛点**: 对日报项目选择不满意时，必须重跑全流程（Surf API ≈100秒/项目, ≈¥2/次）

**方案**:

```
[前端选择已分析项目] → enrichment → 质检 → 总结 → 推文 → 配图 → 日报
                       ↑ 从磁盘读已有策略官报告，跳过 Surf
```

**前置需求**: 需要知道当天哪些项目已有策略官报告。

**改动清单**:

| 文件 | 改动 |
|------|------|
| `daily_report_service.py` | 抽取 `_run_post_analysis_pipeline(projects, analysis_results, ...)` |
| `daily_report_service.py` | 新增 `load_analysis_from_disk(date, twitters)` 从 MD 文件重建 analysis_results |
| `research.py` | 新增 `GET /api/research/available-projects?date=20260309` 返回当天已分析项目列表 |
| `research.py` | 新增 `POST /api/research/reassemble` 接收选中项目，调用后半段管线 |
| `ResearchView.tsx` | 增加「重新组装」按钮 + 项目多选弹窗 |

**边界情况**:
- 重新组装**不写入** Google Sheets（避免覆盖已有记录），只更新本地文件
- 重新组装**不入库** ChromaDB（内容未变，避免重复条目）
- 进度栏跳过侦察+策略步骤，直接从 enrichment 开始显示

---

### F2. 研报入库 + 48h 去重

**痛点**: 项目深度报告 110 份大量重复（Yellow 连续出现 03-08、03-09），浪费 Surf API

**方案**:

```
侦察官候选列表 → 48h去重过滤 → [日志标记被过滤项目] → 策略官分析 → 入库ChromaDB
```

**存储设计**:
- Collection: `research_reports`
- ID 格式: `{twitter}_{date}`（如 `reya_xyz_20260309`），保留全部历史
- metadata: `{ project_name, twitter, date, track, catalyst, summary }`
- document: 策略官报告全文

**去重逻辑**:
- 侦察官筛选完**自动模式**候选项目后，查 ChromaDB 48h 历史
- 48h 内已分析 → 移除并日志标记 `⚠️ [dedup] Yellow 已在 03-08 分析，跳过`
- **用户手动选择的项目不受 48h 限制**（尊重用户意图）

**一次性迁移**: 现有 `reports/research/projects/` 目录的 110 份 MD 文件需要批量导入 ChromaDB

**改动清单**:

| 文件 | 改动 |
|------|------|
| `chroma_service.py` | 新增 `RESEARCH_REPORTS` collection |
| `chroma_service.py` | 新增 `add_research_report(twitter, date, content, metadata)` |
| `chroma_service.py` | 新增 `get_recent_projects(hours=48)` → 返回近48h已分析的twitter列表 |
| `chroma_service.py` | 新增 `get_project_history(twitter)` → 返回项目历史报告列表 |
| `daily_report_service.py` | 侦察后：自动模式调用 48h 过滤 |
| `daily_report_service.py` | 管线结束后：每个报告调用 `add_research_report()` |
| `research.py` | 新增 `GET /api/research/project-history?twitter=xxx` |
| 迁移脚本 | 一次性导入现有 110 份报告到 ChromaDB |

**注意**: F2 入库后，B5（报告文件去重）自动解决 — 48h 过滤阻止了重复分析的发生。

---

### F1 × F2 交互

| 场景 | 行为 |
|------|------|
| 完整跑管线 | 侦察 → 48h过滤 → 策略 → 入库ChromaDB → 后半段管线 |
| 重新组装 | 从磁盘读报告 → 后半段管线，**不入库**（内容不变） |
| 手动选择项目 | 不受48h限制，正常分析，入库 |

---

## 三、P34 遗留修复

### 已实装（待验证）

| # | 任务 | 状态 |
|---|------|------|
| B1 | 催化剂二次校验（`_catalyst_importance()` 评分 > 0） | ✅ 已部署 |
| B6 | 前端进度栏投研模式8步骤 | ✅ 已部署 |

### 待实施

| # | 任务 | 依赖 | 预估 |
|---|------|------|------|
| B2 | 配图动态布局（<6项目） | 无 | 15 min |
| B3 | X 账号 6551 API 验证 | F2（验证后的handle需同步到ChromaDB） | 30 min |
| B4 | 前端催化日期去重 | 无 | 5 min |
| B5 | 报告文件去重 | F2 自动解决 | 0 min |

### B3 × F2 交互

> [!WARNING]
> 6551 验证可能修正 twitter handle（如 `@fake_xxx` → `@real_xxx`）。  
> ChromaDB 入库发生在**验证之后**，所以用的是修正后的 handle，不冲突。  
> 但 F1 重新组装从磁盘读文件时，文件名仍是旧 handle。需要在验证时同步重命名文件。

---

## 四、执行计划

### Phase 1（核心功能）

1. **F1 后端**: 抽取 `_run_post_analysis_pipeline()`，新增 `/reassemble` 和 `/available-projects` 端点
2. **F1 前端**: 重新组装按钮 + 项目多选弹窗
3. **B2**: 配图动态布局

### Phase 2（入库去重）

4. **F2 ChromaDB**: 新增 collection + 入库/查询方法
5. **F2 去重过滤**: 侦察后48h过滤逻辑
6. **F2 迁移**: 批量导入现有110份报告
7. **B4**: 催化日期去重

### Phase 3（账号验证）

8. **B3**: 6551 API 验证 + 文件重命名同步

---

## 五、验证计划

### Phase 1

- [ ] 完整管线 → 点「重新组装」选3/6个项目 → 配图/推文/日报只含选中项目
- [ ] <6项目配图布局正常
- [ ] 催化标签只显示评分系统认可的事件

### Phase 2

- [ ] ChromaDB `research_reports` 有110+条历史数据
- [ ] 连续跑两次管线 → 第二次自动跳过48h内已分析项目
- [ ] 手动选择48h内项目 → 不被过滤，正常分析
- [ ] 查询项目历史返回正确列表

### Phase 3

- [ ] 6551 验证后 handle 准确率 > 90%
- [ ] 验证后文件名和 ChromaDB 条目 handle 一致
