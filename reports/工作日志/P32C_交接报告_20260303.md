# P32-C 智能体提示词统一 - 工作交接报告

> **日期**: 2026-03-03  
> **工作时段**: 约 22:00 - 23:00  
> **阶段**: P32-C 侦察官信源管理 + 提示词统一

---

## 一、今日完成事项

### ✅ C1: 侦察官信源管理 — Google Sheets + Jinja2 统一

**问题**: `scout.jinja2`（P15 可编辑）和 `daily_report_service.py`（硬编码）存在两份冲突 prompt，P15 编辑无效。

**解决**:
- **Google Sheets** 新建 `侦察源` Tab（5 个初始 X 账号，支持增删和启用/禁用）
- **`research_sheet.py`** 新增 `get_scout_sources()` 方法，读取 Sheets 启用的账号
- **`scout.jinja2`** 改为 `{{ accounts }}` + `{{ account_count }}` 变量模板
- **`run_scout()`** 改为读 Sheets 账号 → 注入模板 → 生成 prompt
- 搜索窗口从 7 天改为 **48 小时**

### ✅ C2-C3: 全部智能体提示词统一

**问题**: 策略官、审核官、日报写手、推文写手所有提示词均硬编码在 Python 中，P15 编辑无效。

**解决**:
- 新增共享 `_render_research_template()` 辅助函数
- **策略官** → `analyst.jinja2`，P15 编辑生效
- **审核官** → 新建 `summarizer.jinja2`，P15 编辑生效
- **日报写手** → 新建 `writer.jinja2`，P15 编辑生效
- **推文写手** → `tweet_digest.jinja2`，P15 编辑生效
- 所有智能体均保留 **fallback 兜底**（模板损坏时用硬编码版本）

### ✅ C4: P15 前端全部打通

- 后端 API `RESEARCH_AGENTS` 注册 **6 个**投研智能体（原来 4 个）
- **`ScoutPromptEditor.tsx`** 重写为动态加载组件，6 个 sub-tab：
  - 🔭 侦察官 | 🔬 策略官 | 📋 审核官 | ✍️ 日报写手 | 🐦 推文写手 | 📝 日报模板

### ✅ 侦察官流程验证

- 运行 `test_unified_scout.py`，侦察官成功发现 **7 个项目**
- 验证 Sheets 信源读取、模板渲染、Surf API 调用、项目解析全部正常
- 完整管道测试因 **Surf API 余额不足** 暂未完成

---

## 二、变更文件清单

### 后端
| 文件 | 变更 |
|------|------|
| `services/research_sheet.py` | 新增 `SCOUT_TAB_NAME`、`get_scout_sources()` |
| `services/daily_report_service.py` | 新增 `_render_research_template()`；5 个智能体改为模板读取 |
| `api/research.py` | `RESEARCH_AGENTS` 扩展为 6 个 |
| `data/prompts/research/scout.jinja2` | 改为 `{{ accounts }}` 变量，48H 窗口 |
| `data/prompts/research/summarizer.jinja2` | **[NEW]** 审核官 prompt |
| `data/prompts/research/writer.jinja2` | **[NEW]** 日报写手 prompt |

### 前端
| 文件 | 变更 |
|------|------|
| `settings/components/ScoutPromptEditor.tsx` | 重写为动态 6-tab 编辑器 |

### 脚本
| 文件 | 说明 |
|------|------|
| `scripts/setup_scout_sources.py` | 创建 Sheets 侦察源 Tab |
| `scripts/test_unified_scout.py` | 统一侦察流程测试 |
| `scripts/test_full_pipeline.py` | 完整管道测试（绕过 uvicorn） |

### Google Sheets
| Tab | 说明 |
|-----|------|
| `侦察源` | **[NEW]** 5 个 X 账号（@leakmealpha 等），支持启用/禁用 |

---

## 三、下一步工作

| 优先级 | 任务 |
|--------|------|
| 🔴 高 | **充值 Surf API** → 跑完整管道验证（策略官→审核官→写手→推文） |
| 🟡 中 | 关掉多余的 uvicorn 实例（当前有 2 个抢端口 8000） |
| 🟡 中 | Sheets 连接偶尔失败（get_scout_sources 降级到 fallback），排查凭证 |
| 🟢 低 | P15 可考虑新增"变量说明"提示（哪些变量可用、作用） |

---

## 四、关键设计决策

| 决策 | 结论 | 原因 |
|------|------|------|
| 信源管理方式 | Google Sheets | 无需改代码/部署，手机可操作 |
| 模板缺失策略 | fallback 硬编码 | 保证管道鲁棒性 |
| P15 前端方案 | 动态 API 驱动 | 后端加智能体前端自动出现 |
| 搜索窗口 | 48 小时 | 用户要求，提高时效性 |

---

## 五、运行环境

| 服务 | 端口 | 状态 |
|------|------|------|
| Backend (uvicorn) | 8000 | ⚠️ 有 2 个实例冲突，需关掉旧的 |
| Frontend (Next.js) | 3000 | ✅ 运行中 |
| Surf API | — | ❌ 余额不足 |
