# L4 升级方案 - 详细实施计划

> **版本**: v1.0  
> **日期**: 2026-01-13  
> **状态**: 待审批  
> **负责人**: Claude

---

## 📋 项目概述

将 P12 清洗工具升级为工业级版本 (v2.0)，实现异步并发、断点续传、智能去重等功能。

---

## 🗓️ 实施阶段

### Phase 1: 核心升级 (Day 1)

> **目标**: 完成基础功能升级，达到可用状态

| 序号 | 任务 | 预计工时 | 依赖 |
|:----:|:-----|:--------:|:----:|
| 1.1 | 备份当前 `cleaner_cli.py` | 5 min | - |
| 1.2 | 重写为 AsyncIO 版本 | 1 hour | 1.1 |
| 1.3 | 实现智能分片 `chunk_text()` | 30 min | 1.2 |
| 1.4 | 实现断点续传 (文件级) | 30 min | 1.2 |
| 1.5 | 实现 MD5 去重 (本次运行) | 15 min | 1.2 |
| 1.6 | 单元测试: 分片功能 | 15 min | 1.3 |
| 1.7 | 集成测试: 端到端清洗 | 30 min | All |

**Phase 1 交付物**:
- `backend/tools/cleaner_cli.py` v2.0
- `backend/tools/processed_log.json` (运行时生成)

---

### Phase 2: 健壮性增强 (Day 2-3)

> **目标**: 解决潜在风险，提升稳定性

| 序号 | 任务 | 预计工时 | 优先级 |
|:----:|:-----|:--------:|:------:|
| 2.1 | 添加 API 限流自动重试 (tenacity) | 30 min | 🔴 高 |
| 2.2 | 添加 Lark 端去重 (查询已存在) | 1 hour | 🔴 高 |
| 2.3 | 添加超长段落二次切分 | 30 min | 🟡 中 |
| 2.4 | 添加详细执行日志 | 30 min | 🟡 中 |
| 2.5 | 大文件压力测试 (50万字) | 1 hour | 🔴 高 |

**Phase 2 交付物**:
- 更新后的 `cleaner_cli.py` v2.1
- 测试报告

---

### Phase 3: 文档与收尾 (Day 3)

> **目标**: 更新文档，完成交接

| 序号 | 任务 | 预计工时 |
|:----:|:-----|:--------:|
| 3.1 | 更新 PROJECT_HANDBOOK.md | 15 min |
| 3.2 | 编写使用说明 | 15 min |
| 3.3 | 创建工作交接报告 | 15 min |

---

## 🔧 技术实现细节

### 1. 异步并发实现

```python
from openai import AsyncOpenAI
import asyncio

CONCURRENCY_LIMIT = 5
semaphore = asyncio.Semaphore(CONCURRENCY_LIMIT)

async def process_single_chunk(chunk, ...):
    async with semaphore:
        response = await client.chat.completions.create(...)
        return parse_response(response)
```

### 2. Lark 端去重方案

```python
async def check_exists_in_lark(content_hash: str) -> bool:
    """查询 Lark 是否已存在相同内容"""
    # 方案 A: 在 Lark 表格中添加 content_hash 字段
    # 方案 B: 维护本地持久化 hash 文件
    pass

async def upload_if_not_exists(snippet, author, style):
    h = get_content_hash(snippet.clean_text)
    if await check_exists_in_lark(h):
        return False  # 跳过
    return await upload_to_lark_async(snippet, author, style)
```

### 3. 自动重试机制

```python
from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type
from openai import RateLimitError, APITimeoutError

@retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=2, max=60),
    retry=retry_if_exception_type((RateLimitError, APITimeoutError))
)
async def call_llm_with_retry(client, messages, ...):
    return await client.chat.completions.create(...)
```

---

## 📊 测试计划

| 测试类型 | 用例 | 预期结果 |
|:---------|:-----|:---------|
| **单元测试** | `chunk_text()` 空输入 | 返回空列表 |
| **单元测试** | `chunk_text()` 短文本 | 返回单元素列表 |
| **单元测试** | `chunk_text()` 无 `\n\n` | 正常切分 |
| **集成测试** | 单文件清洗 | 成功上传 Lark |
| **集成测试** | 多文件并发 | 5 并发正常 |
| **集成测试** | 中断恢复 | 断点续传有效 |
| **压力测试** | 50万字大文件 | 分片正常，无截断 |
| **异常测试** | 模拟 API 429 | 自动重试成功 |

---
![alt text]
## ⚠️ 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|:-----|:------:|:----:|:---------|
| DeepSeek API 限流 | 高 | 中 | Phase 2 添加 tenacity 重试 |
| Lark 数据重复 | 高 | 高 | Phase 2 实现 Lark 去重 |
| 超长段落截断 | 中 | 中 | Phase 2 添加二次切分 |
| 网络中断 | 低 | 中 | Phase 1 断点续传已覆盖 |

---

## ✅ 验收标准

| 编号 | 验收项 | 标准 |
|:----:|:-------|:-----|
| AC1 | 性能提升 | 200万字 < 30 分钟 |
| AC2 | 断点续传 | 中断后重跑自动跳过已处理文件 |
| AC3 | 去重有效 | 本次运行无重复上传 |
| AC4 | 稳定性 | 连续 3 次运行无异常 |
| AC5 | 兼容性 | 现有 CLI 参数不变 |

---

## 📦 准备清单

- [ ] 备份 `backend/tools/cleaner_cli.py`
- [ ] 准备测试数据文件 (50万字长文)
- [ ] 确认 DeepSeek API QPM/TPM 限制
- [ ] 创建回滚脚本

---

## 📅 时间线

```
Day 1 (2026-01-14): Phase 1 核心升级
  └─ 09:00-12:00 实现核心功能
  └─ 14:00-16:00 单元测试 + 集成测试

Day 2 (2026-01-15): Phase 2 健壮性增强
  └─ 09:00-12:00 添加重试 + Lark 去重
  └─ 14:00-17:00 压力测试

Day 3 (2026-01-16): Phase 3 收尾
  └─ 09:00-12:00 文档更新 + 交接
```

---

**请审批后开始实施。**

---

*— Claude, 2026-01-13*
