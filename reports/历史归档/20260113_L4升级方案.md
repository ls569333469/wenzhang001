# P12.1 - 工业级清洗工具升级 最终方案

> **版本**: v1.0 (Final Merged)  
> **日期**: 2026-01-12  
> **状态**: 待审批

---

## 🎯 目标

将 **工业级清洗优化方案** 与 **当前 P12 实现** 合并，打造一个：
- ⚡️ **高速** (200万字 → 15-20分钟)
- 🛡️ **稳定** (断点续传 + 自动重试)
- 📉 **低成本** (MD5 去重)
- 🧠 **高质量** (Shell-Kernel 双 Prompt)

的工业级数据清洗工具。

---

## 📋 最终架构

```
cleaner_cli.py (v2.0 Async)
├── 配置层
│   ├── load_api_config()          # 从 user_config.json 读取 API Key
│   └── CONCURRENCY_LIMIT = 5      # 并发控制
├── 模型层
│   └── AsyncLLMProvider           # 异步 LLM 适配器 (Doubao/DeepSeek/Gemini)
├── 处理层
│   ├── chunk_text()               # 智能分片 (3000字符/片，按段落切分)
│   ├── get_content_hash()         # MD5 去重
│   ├── process_single_chunk()     # 处理单个分片 (异步)
│   └── process_file()             # 处理单个文件 (异步)
├── 持久层
│   ├── load_checkpoint()          # 断点读取
│   ├── save_checkpoint()          # 断点保存
│   └── upload_to_lark_async()     # 异步 Lark 上传
└── CLI 入口
    └── clean()                    # 主命令
```

---

## 🔧 核心改动

### 1. 异步并发 (来自工业级方案)

```python
from openai import AsyncOpenAI
import asyncio

CONCURRENCY_LIMIT = 5
semaphore = asyncio.Semaphore(CONCURRENCY_LIMIT)

async with semaphore:
    response = await client.chat.completions.create(...)
```

### 2. 智能分片 (来自工业级方案)

```python
def chunk_text(text: str, max_chars: int = 3000) -> List[str]:
    """按段落切分长文本，避免截断"""
    if len(text) <= max_chars:
        return [text]
    
    chunks = []
    current = ""
    for para in text.split('\n\n'):
        if len(current) + len(para) < max_chars:
            current += para + "\n\n"
        else:
            if current:
                chunks.append(current.strip())
            current = para + "\n\n"
    if current:
        chunks.append(current.strip())
    return chunks
```

### 3. 断点续传 (来自工业级方案)

```python
CHECKPOINT_FILE = "processed_log.json"

def load_checkpoint() -> set:
    if os.path.exists(CHECKPOINT_FILE):
        with open(CHECKPOINT_FILE, "r") as f:
            return set(json.load(f))
    return set()

def save_checkpoint(processed: set):
    with open(CHECKPOINT_FILE, "w") as f:
        json.dump(list(processed), f)
```

### 4. MD5 去重 (来自工业级方案)

```python
import hashlib

seen_hashes: Set[str] = set()

def get_content_hash(text: str) -> str:
    return hashlib.md5(text.strip().encode('utf-8')).hexdigest()

# 使用
h = get_content_hash(snippet.clean_text)
if h in seen_hashes:
    continue  # 跳过重复
seen_hashes.add(h)
```

### 5. Shell-Kernel 双 Prompt (保留当前方案)

```python
if source_category.lower() == "shell":
    system_prompt = SHELL_MINER_PROMPT.format(author=author)
else:
    system_prompt = KERNEL_FILTER_PROMPT
```

### 6. user_config.json 读取 (保留当前方案)

```python
def load_api_config() -> dict:
    config_path = Path(__file__).parent.parent / "config" / "user_config.json"
    if config_path.exists():
        with open(config_path, "r") as f:
            return json.load(f)
    return {}
```

---

## 📊 预期效果对比

| 指标 | 当前 v1.0 | 升级后 v2.0 |
|:-----|:----------|:------------|
| **200万字处理时间** | 3-5 小时 | **15-20 分钟** |
| **断点续传** | ❌ | ✅ |
| **重复过滤** | ❌ | ✅ MD5 |
| **长文处理** | ⚠️ 截断 | ✅ 分片 |
| **并发数** | 1 | 5-10 可调 |

---

## 🗂️ 文件变更清单

| 文件 | 操作 | 说明 |
|:-----|:-----|:-----|
| `backend/tools/cleaner_cli.py` | **重写** | 升级为 Async v2.0 |
| `backend/tools/processed_log.json` | **新增** | 断点记录 (运行时自动生成) |

---

## 🖥️ 使用方式 (不变)

```bash
 
python -m tools.cleaner_cli clean \
  --folder data/mimeng \
  --author 咪蒙 \
  --style mimeng \
  --source-category Shell

# Kernel 模式 - 清洗 Web3 快讯
python -m tools.cleaner_cli clean \
  --folder data/web3_flash \
  --author 链捕手 \
  --style lianbushou \
  --source-category Kernel
```

---

## ⚠️ 注意事项

1. **CONCURRENCY_LIMIT**: 默认 5，如果 DeepSeek 报 429 错误，降低到 3
2. **断点文件**: `processed_log.json` 会记录已处理的文件，重跑时自动跳过
3. **去重范围**: 仅本次运行有效，跨次运行的去重需要依赖 Lark

---

**请审批后开始实施。**
